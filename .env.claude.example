# Claude Code Configuration Example
# Copy this file to .env.claude and fill in your credentials

# Service Desk Plus Cloud API Configuration
# Get these from Zoho API Console: https://api-console.zoho.com/
SDP_CLIENT_ID=your_client_id_here
SDP_CLIENT_SECRET=your_client_secret_here

# Your Service Desk Plus portal URL (without /app/ or /api/)
# Example: https://helpdesk.company.com
SDP_BASE_URL=https://your-portal-domain.com

# Instance name from Admin > Instance Settings
# This is used in the API path: /app/{instance_name}/api/v3/
SDP_INSTANCE_NAME=your-instance

# OAuth Refresh Token (obtained via Self Client setup)
# Run: node scripts/setup-self-client.js to generate
SDP_REFRESH_TOKEN=your_refresh_token_here

# SSE Server Configuration
# These control how the MCP server runs
SDP_HTTP_PORT=3456  # Port for SSE server
SDP_HTTP_HOST=127.0.0.1  # Host to bind to
SDP_API_KEYS=your-api-key-here  # API key for authentication
SDP_ENABLE_CORS=true  # Enable CORS for browser clients
SDP_CORS_ORIGIN=*  # CORS allowed origins

# Features (adjust based on your needs)
SDP_USE_DB_TOKENS=false  # Disable database for Claude Code
SDP_USE_AUDIT_LOG=false  # Disable audit logging
SDP_USE_CHANGE_TRACKING=false  # Disable change tracking

# Optional: Default technician for operations
SDP_DEFAULT_TECHNICIAN_EMAIL=your-email@company.com